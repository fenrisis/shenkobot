apiVersion: v1
kind: Secret
metadata:
  name: bot-secret
type: Opaque
stringData:
  # ВАЖНО: Это TEMPLATE для примера. НЕ коммитить реальные токены!
  #
  # Перед первым применением создать настоящий secret руками:
  # kubectl create secret generic bot-secret -n shenkobot \
  #   --from-literal=BOT_TOKEN=<TELEGRAM_BOT_TOKEN_FROM_BOTFATHER> \
  #   --from-literal=DB_PASSWORD=<SAME_AS_POSTGRES_SECRET>
  #
  # После создания удалить этот файл из применения или закомментировать в kustomization.yaml
  BOT_TOKEN: <REPLACE_WITH_TELEGRAM_BOT_TOKEN>
  DB_PASSWORD: <REPLACE_WITH_DB_PASSWORD>
